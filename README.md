#🎬 Hermes 背景视频插件 · Ver.1.5

为 Hermes 桌面客户端添加**全屏循环背景视频**——视频以 20% 可见度铺满界面背景，不影响 UI 可读性；左右侧栏自动半透明透出视频；带完整图形化设置页。

> BETA 版新增：**视频内容在窗口内自由移动**（0%~100%，无黑边）——像壁纸的"填充方式"一样调整画面位置，窗口始终全屏。
> 1.2版本新增：提高了兼容性，增加了右上角调试徽标隐藏功能
> 1.3版本新增：现在可以替换照片了，支持jpg和png格式
> 1.5版本新增：
>   - **统一媒体控制台**：路径输入框智能识别文件夹或具体文件，自带「选文件夹…」「选文件…」「恢复默认」原生对话框按钮。
>   - **多层级递归扫描**：自动扫描选择目录下所有子文件夹中的图片与视频。
>   - **媒体分类与网格实时预览**：支持按「全部 / 图片 / 视频」分类筛选，缩略图直观显示、视频悬停预览，点击卡片即刻切换背景。

演示视频：
Bilibili: 【能把任意本地视频变成hermes客户端皮肤的插件——可能是目前自定义程度最高的agent动态皮肤？-哔哩哔哩】 https://b23.tv/cuQXvFE

## ✨ 功能一览

- 🌅 在hermes界面全屏循环背景视频，20% 可见度，静音播放，不抢 UI
- 🎨 左右侧边栏半透明（默认深蓝 60%），透出底部视频
- 🖼 **视频内容水平位置可调**（0% 靠左 / 50% 居中 / 100% 靠右）
- ⚙️ **提供图形化设置页**（侧边栏「背景视频」入口，或 `Ctrl+K` 搜"背景视频"）：
  <img width="1811" height="1080" alt="image" src="https://github.com/user-attachments/assets/79544cc1-39af-493f-b017-bfa48cc9cb72" />
  - 启用 / 关闭背景视频
  - 视频文件路径（支持中文/空格）（支持mp4 jpg png）
  （这里直接把你要调用的视频目录复制进来就行，记得最后以你要调用的视频全名结尾，千万要带扩展名！！）
  - 视频可见度（5%~50%）
  - 视频水平位置（0%~100%，拖动实时预览）
  - 侧边栏透明度（30%~100%）
- 💾 设置自动保存，重启后依然生效
- 🔄 实时预览：拖动滑块立即生效，无需重启客户端（也不用点“保存并应用”）

## 📦 安装

1. 关闭 Hermes 桌面客户端
2. 将 `bg-video` 文件夹整个复制到：
   ```
   %LOCALAPPDATA%\hermes\desktop-plugins\
   ```
   最终路径：
   ```
   %LOCALAPPDATA%\hermes\desktop-plugins\bg-video\plugin.js
   ```
3. 重新打开 Hermes 桌面客户端
4. 左侧边栏出现「背景视频」入口 → 点击打开设置页

> 若入口未出现：`Ctrl+K` → 输入 `Reload desktop plugins` → 回车

## 🔧 使用

- **开箱即用**：插件内置 `bg-default.mp4`（绝区零视频），无需任何配置
- **换自己的视频**：设置页 → 视频文件路径 → 填本地视频绝对路径 → 保存并应用
- **恢复默认**：清空路径输入框 → 保存（自动回退到自带视频）
- **调整画面位置**：设置页 → 视频水平位置 → 拖动滑块实时预览

## 🗑 卸载

删除 `%LOCALAPPDATA%\hermes\desktop-plugins\bg-video\` 文件夹即可，无任何残留。

## 📋 兼容性

- 平台：Windows 优先（macOS/Linux 理论上可用，安装时需要根据系统实际情况调整安装位置）
- 客户端：Hermes Desktop v0.20.x
- 视频格式：MP4 / WebM 等浏览器原生支持格式

## 📁 文件说明

| 文件 | 说明 |
|---|---|
| `bg-video/plugin.js` | 插件主体（设置页 + 背景注入 + 侧栏半透明） |
| `bg-video/bg-default.mp4` | 默认背景视频（绝区零，约 9.3MB） |
| `README.md` | 本说明 |

## ⚠️ BETA 注意事项

- 目前的版本只适配暗色外观模式（界面右上角设置➡️外观➡️主题右侧选择暗色），明亮模式会导致显示有问题
- 视频解码消耗少量 GPU/CPU，低配设备可调低"视频可见度"或关闭
- 侧栏半透明为自动检测实现，个别界面主题下可能有不完美表现，欢迎反馈
- 已知限制：视频自动播放需静音（浏览器策略），背景视频不会带声音

---
*Created by LIANG. Using Hermes agent, powered by deepseekV4 flash and Hy3 · Hermes 桌面背景视频系列 Ver.1.3 BETA · 欢迎反馈与 PR*
